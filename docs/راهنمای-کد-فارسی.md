# راهنمای خواندن کد — سامانه مدیریت فرش

این سند برای کسی است که کدنویسی بلد نیست ولی می‌خواهد بداند **چه چیزی کجا و چرا** اتفاق می‌افتد. روی هر فایل منبع هم توضیح فارسی (`///` یا `//`) گذاشته شده است.

## جریان کلی یک درخواست

```
مرورگر (Vue)  →  API (Controllers)  →  سرویس (Application)  →  دیتابیس (Infrastructure)
```

مثال: «تکمیل مرحله قالیشویی فرش»

1. کاربر در جزیرهٔ `RugWorkflowPanel.vue` دکمهٔ تکمیل را می‌زند.
2. `api.post('/api/rugs/.../advance')` با کوکی نشست و هدر `X-CSRF-TOKEN` فرستاده می‌شود.
3. `RugsApiController` شناسه کارگاه را از claimهای کوکی می‌خواند.
4. `RugManagementService` فرش را از دیتابیس بارگذاری می‌کند.
5. `WorkflowEngine.AdvanceStepAsync` وضعیت مرحله را Completed می‌کند و مرحله بعد را InProgress می‌کند.
6. `SaveChanges` در PostgreSQL ذخیره می‌کند.
7. پاسخ JSON (RugDto) به Vue برمی‌گردد و صفحه به‌روز می‌شود.

## لایه‌ها (پوشه `src/`)

| پوشه | نقش | مثال |
|------|-----|------|
| **Domain** | موجودیت‌ها و قوانین ثابت دامنه | `Rug`, `WorkflowTemplate` |
| **Application** | منطق کار؛ بدون SQL مستقیم | `WorkflowEngine`, `CostCalculationService` |
| **Infrastructure** | EF Core، Repository، ذخیرهٔ فایل | `AppDbContext`, `RugRepository` |
| **Web** | HTTP، صفحات Razor، امنیت | `RugsController`, `Program.cs` |

## مفاهیم مهم

- **Tenant (کارگاه)**: هر مشتری شما یک کارگاه جدا؛ فرش و قالب‌ها فقط برای همان کارگاه.
- **ProcessStepType**: نوع کار (قالیشویی، دارکشی، …) — در کل سیستم یکسان.
- **WorkflowTemplate**: ترتیب مراحل از پیش تعریف‌شده برای کارگاه.
- **RugWorkflowStep**: نمونهٔ واقعی یک مرحله روی یک فرش مشخص.
- **WorkflowEngine**: تعیین می‌کند کدام مرحله فعال است و هزینه چطور جمع می‌شود.

## فرانت (`src/RugsManagment.Web/ClientApp/src/`)

صفحه‌ها Razor هستند و فقط بخش‌های تعاملی «جزیرهٔ» Vue می‌شوند
(`<div data-island="rug-workflow">` در view، ثبت‌شده در `islands/registry.ts`).
خروجی Vite در `wwwroot/dist` می‌نشیند و `ViteAssets.cs` از روی manifest می‌خواندش.

| پوشه | نقش |
|------|-----|
| `islands/` | جزیره‌های Vue (پنل مراحل، گالری، فروش، …) |
| `lib/api.ts` | ارتباط با سرور + توکن CSRF + صف آفلاین |
| `components/` | قطعات تکرارشونده (آیکون، ورودی مبلغ، پالت دستور) |
| `styles/app.css` | توکن‌های Tailwind v4 و حالت تاریک |

## حساب‌های اولیه

پس از اولین اجرا، `DatabaseSeeder` دادهٔ نمونه می‌سازد — جزئیات در همان فایل با کامنت فارسی.

## محاسبه هزینه

**فقط** در `CostCalculationService.cs` — فرانت فقط عدد را نمایش می‌دهد (`faMoney` در `lib/format.ts`).
